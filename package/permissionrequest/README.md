# PermissionRequest

```yaml
deployToken: gitlab+deploy-token-1227231
password: bYc37BysSdV7r3TnxnZ-
```

## Usage

```dart
BlankPermission() // To make an entirely custom permission page.

GeolocPermission() // Sketch geolocation permission page with backend customizable.
NotificationsPermission() // Sketch notifications permission page with backend customizable.

PermissionsPageView() // Sketch PageView using to two widgets above.
PermissionsPageViewCustom() // PageView wrapper ready for a list of permissions widgets.
```
